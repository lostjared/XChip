
project(Emulator)
FILE(GLOB_RECURSE SRC ./*.cpp)
ADD_EXECUTABLE(${PROJECT_NAME} ${SRC})
ADD_DEPENDENCIES(${PROJECT_NAME} Utility Core SDLMedia)
TARGET_LINK_LIBRARIES(${PROJECT_NAME} Utility Core SDLMedia)
